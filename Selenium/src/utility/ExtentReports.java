package utility;


import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Test;

import com.aventstack.extentreports.ExtentTest;
//import com.aventstack.extentreports.reporter.ExtentReporter;
//import com.aventstack.extentreports.reporter.ExtentSparkReporter;
import com.aventstack.extentreports.reporter.configuration.Theme;

import basePackage.BaseTestPage;

public class ExtentReports  {/*
	public ExtentSparkReporter htmlReporter;
	public ExtentReports extent;
	public ExtentTest test;	
	public ExtentReports extented = new ExtentReports();
	extented.attachReporter(reporterType);
	ExtentReporter avent = new ExtentReporter("/user/build/");
	ExtentReporter klov = new ExtentReporter("project", "build");
		
	@BeforeTest
	public void setReports() {
		
	

		ExtentReports extent = new ExtentReports();
		extent.attachReporter(avent);
		extent.attachReporter(klov);
		
		ExtentSparkReporter reporter = new ExtentSparkReporter("./ExtentReports/FirstReport.html");
		reporter.config().setDocumentTitle("Extent Report");
		reporter.config().setReportName("Automation Report");
		reporter.config().setTheme(Theme.DARK);
		
		extent = new ExtentReports();
		extent.html
		
		
		// Look & feel
		htmlReporter= new ExtentSparkReporter("./ExtentReports/FirstReport.html");
		htmlReporter.config().setDocumentTitle("Extent Report");
		htmlReporter.config().setReportName("Functional Report");
		htmlReporter.config().setTheme(Theme.DARK);
		
		// Create entries + add additional info like user name, env name, browser name etc by using extent
		extent = new ExtentReports();
		extent.(htmlReporter);
		extent.setReports();
		//(htmlReporter);	
	
	}

	@AfterTest
	public void endReport() {
		extentR.htmlReporter.flush();
	}
	*/
	
	
	
}
